{
  "name": "trafo360-v2",
  "version": "0.1.0",
  "private": true,
  "description": "TRAFO 360 v2 - Manufacturing Project Workflow & Document Management System",
  "workspaces": [
    "apps/api",
    "apps/worker",
    "apps/web",
    "libs/shared"
  ],
  "scripts": {
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=prisma/schema.prisma",
    "prisma:migrate:deploy": "prisma migrate deploy --schema=prisma/schema.prisma",
    "prisma:seed": "tsx prisma/seed.ts",
    "prisma:studio": "prisma studio --schema=prisma/schema.prisma",
    "dev:api": "npm run start:dev --workspace=apps/api",
    "dev:worker": "npm run start:dev --workspace=apps/worker",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=libs/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/worker && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=apps/api && npm run test --workspace=libs/shared",
    "typecheck": "npm run typecheck --workspace=apps/api && npm run typecheck --workspace=apps/worker && npm run typecheck --workspace=apps/web && npm run typecheck --workspace=libs/shared"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "embedded-postgres": "^18.4.0-beta.17",
    "prisma": "^5.19.1",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcryptjs": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
